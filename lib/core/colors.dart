import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Background
  static const Color primaryColor = Color(0xFF02020A);

  // Surfaces
  static const Color blueColor = Color(0xFF101026);

  // Accent Colors
  static const Color redColor = Color(0xFFE61E40);
  static const Color greenColor = Color(0xFF53C687);

  // Neutral Colors
  static const Color greyColor = Color(0xFF6A7282);
  static const Color whiteColor = Color(0xFFFFFFFF);
  static const Color blackColor = Color(0xFF000000);
}
